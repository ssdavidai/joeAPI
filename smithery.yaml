# Smithery MCP Server Configuration
# Documentation: https://smithery.ai/docs/build/project-config/smithery-yaml

runtime: "typescript"

# Default environment variables
# Users can override these when they install the server
env:
  JOEAPI_BASE_URL: "https://joeapi.fly.dev"
