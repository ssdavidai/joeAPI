# Smithery MCP Server Configuration
# Documentation: https://smithery.ai/docs/build/project-config/smithery-yaml

runtime: "typescript"

# Note: Update JOEAPI_BASE_URL to your production API URL before deployment
# Users will be able to configure this when they install the server
